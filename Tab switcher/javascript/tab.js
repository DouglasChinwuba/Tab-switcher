/**
 * Tab class
 */
class Tab {
  constructor(windowId, tabId, title, url, icon) {
    this.windowId = windowId;
    this.tabId = tabId;
    this.title = title;
    this.url = url;
    this.icon = icon;
  }
}
